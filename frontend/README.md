# AI Helpdesk Frontend

React + TypeScript + Vite로 구현한 AI Helpdesk 관리자 Dashboard입니다.

```bash
npm install
npm run dev
```

기본 Backend 주소는 `http://127.0.0.1:8000`입니다. 변경하려면 `.env.example`을 참고해 `VITE_API_BASE_URL`을 설정합니다.

```bash
npm run lint
npm run build
```
